---
import { locales } from '../config'

const currentLocale = Astro.currentLocale
---

{
  locales.map((locale) => {
    const property = `og:locale${locale !== currentLocale ? ':alternate' : ''}`
    return <meta {property} content={locale} />
  })
}
